import React from 'react'; 

export const SurveyResults = ({ surveyResposes, surveyTakers }) => {
  const surveyTakersList = surveyResponses.filter(response => surveyTakers.includes(response.employees_id)); 

  const surveyTakers
}